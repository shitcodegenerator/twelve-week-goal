## Week 1｜戰役設計 + 專案骨架 + 工程化底座（Kickoff）

### 本週目標

把戰場搭好：你之後 11 週都在同一個 repo 演進，並開始累積「文件/決策紀錄」這個 Glassdoor 常見加分項。

### 你要學的技能

- Next.js App Router 基本結構：route segments、layout、error/loading、not-found
    
- TypeScript 專案設定：strict mode、tsconfig 基本策略
    
- 工程化：ESLint/Prettier、git hooks（可選）、commit 規範（可簡化）
    
- 文件化習慣：README + ADR（Architecture Decision Record）框架
    

### 交付成果

- Repo 初始化：`app/` 三頁（Dashboard/Reports/Settings）空殼
    
- README v0：目標、功能範圍、架構草圖
    
- ADR-000：技術選型（Next.js + TS + Postgres + 部署策略）
    

### AI 協助

- Claude Code：產出「專案骨架 PR」（路由、資料夾、基本 lint）
    
- ChatGPT：幫你把作品題目收斂成 MVP（10 條內）+ 非功能需求（security/perf/cost）
    

---

## Week 2｜React Fundamentals + TypeScript 深度前移（從 Vue 轉軌）

### 本週目標

把 React+TS 練到能寫 production UI，不只會「跑得動」。

### 你要學的技能（前端核心 + TS）

- React：JSX、props/state、事件、條件/列表渲染、表單 controlled inputs
    
- Hooks：useState/useEffect（依賴與 cleanup）、useMemo/useCallback（何時需要）
    
- TypeScript：union/narrowing、泛型 props、utility types（Partial/Pick/Omit/Record）
    
- Component 介面設計：可重用、可測、可維護
    

### 交付成果

- 3 個可重用元件（TS typing 必須完整）：
    
    1. DataTable（columns + row type generic）
        
    2. Modal
        
    3. Form（含 validation state）
        
- 「Vue → React 對照筆記」：watch/effect、computed/memo、slot/children
    

### AI 協助

- Claude Code：把你寫的元件做「型別補強」與重構（移除 any）
    
- ChatGPT：產出「React 常見踩坑清單 + 修正策略」
    

---

## Week 3｜Next.js Core + Data Fetching/Caching + Docker 起步

### 本週目標

你要建立 Next.js 的正確心智模型（Server/Client 邊界），並把 Docker 雛形做出來（職缺常見要求）。

### 你要學的技能

- Next App Router：Server Component vs Client Component 邊界與限制
    
- Data Fetching：server fetch、client fetch 的取捨
    
- Caching/Revalidation 的基本概念（先會用最小集合）
    
- Middleware 入門（auth gate / rewrite 的用途）
    
- **Docker 基礎**：Dockerfile（build/run）、.dockerignore、環境變數注入策略
    

### 交付成果

- Dashboard 列表：Server-side fetch 渲染 + Client-side 篩選（state）
    
- Dockerfile v1：可在本機 `docker build` / `docker run` 啟動
    

### AI 協助

- ChatGPT：判斷某頁面應該 server render 還是 client render（用 trade-off 解釋）
    
- Claude Code：生成 Dockerfile 並提醒你 secrets 不要 bake 進 image
    

---

## Week 4｜Backend 基礎 1：REST API 設計 + PostgreSQL（CRUD 跑通但要“像樣”）

### 本週目標

做出職缺常見的「能設計 API + 能落 DB + 能把資料串起來」。

### 你要學的技能（後端/DB關鍵字）

- RESTful API design：resource 命名、status codes、error semantics
    
- **Schema validation**：Zod（request/response schema）
    
- Pagination：offset 或 cursor（二選一做到完整）
    
- PostgreSQL：
    
    - Data modeling（entities/relations）
        
    - Indexing（B-tree、複合索引的基本概念）
        
    - Migration（Prisma migrate 或等價工具）
        
- Database access layer：Repository pattern（或 service 層）
    

### 交付成果

- 至少 2–3 個 domain entities 的 CRUD（例：Campaign / Content / Report）
    
- 統一 API error format（error_code/message/request_id）
    
- Postgres schema + migration 可重現（README 寫清楚如何初始化）
    

### AI 協助

- ChatGPT：當 API reviewer（一致性、語意、擴展性、錯誤碼）
    
- Claude Code：生成 route + validation + DB access（你做 code review）
    

---

## Week 5｜AuthN/AuthZ + Security 基礎（Session/Cookie + JWT/OAuth trade-off）

### 本週目標

讓作品具備：登入、權限、基本防護，並能回答面試追問。

### 你要學的技能（安全關鍵字）

- Authentication：
    
    - session-based auth（cookie httpOnly/secure/sameSite）
        
    - JWT 的使用情境與風險（token lifecycle）
        
    - OAuth 概念（第三方登入流程、scopes）
        
- Authorization：
    
    - RBAC（role-based access control）
        
- Security basics（OWASP 常見面）：
    
    - input validation、XSS/CSRF 基本防法
        
    - secrets management（env、最小暴露面）
        
- Rate limiting / throttling（先做最小版）
    

### 交付成果

- Login/Logout + protected routes（Settings 僅特定角色可看）
    
- RBAC middleware / guard（前後端都要有）
    
- Auth trade-off 筆記：為何選 session vs JWT（面試版 10 行）
    

### AI 協助

- ChatGPT：扮演面試官追問（為何不用 JWT/OAuth？你的 CSRF 怎麼處理？）
    
- Claude Code：補齊 guard/middleware 並檢查 cookie 安全屬性
    

---

## Week 6｜DevOps/SRE 基礎 1：CI/CD 最小實作 + Observability（logs）

### 本週目標

你要有「可部署、可回滾思維、可觀測」的最小集合，並把 CI 做起來（職缺常見）。

### 你要學的技能（DevOps/SRE用語）

- CI pipeline：lint → typecheck → build（test 可先 very minimal）
    
- Release 概念：preview vs production、rollback 思路
    
- Observability 基礎：structured logging、log levels、request_id/correlation id
    
- Health check endpoint（readiness/liveness 概念）
    

### 交付成果

- CI workflow（跑 lint/typecheck/build）
    
- API 統一錯誤處理器 + 結構化 log（至少：path/status/latency/request_id）
    
- `/health` endpoint
    

### AI 協助

- ChatGPT：產出「部署檢查清單」與「故障排查 SOP」
    
- Claude Code：生成 CI YAML + 統一 error handler（你檢查 secrets、cache）
    

---

## Week 7｜System Design 1：用你的作品做架構演練（High-level + bottlenecks）

### 本週目標

把你做的東西「講得像工程師」，能清楚說 bottleneck、trade-offs。

### 你要學的技能（System Design用語）

- Requirement clarification（functional + non-functional）
    
- High-level architecture：client → web → API → Postgres → (cache/queue optional)
    
- Data model 說法：entities、relationships、hot path queries
    
- Bottlenecks：DB contention、N+1、cold start、external dependency
    
- Trade-offs：complexity vs time-to-market、consistency vs availability（入門）
    

### 交付成果

- 1 張架構圖 + 1 份 3–5 分鐘講稿（含 trade-offs）
    
- 口述演練錄音 1 次（自我修正）
    

### AI 協助

- ChatGPT：當面試官追問（你如何擴展？哪裡會成為單點故障？）
    
- Claude Code：把你的架構圖文字轉成更清楚的 bullet
    

---

## Week 8｜Backend 強化：PostgreSQL 效能 + 交易/一致性 + Concurrency 基礎

### 本週目標

把「會用 Postgres」提升到「懂怎麼讓它撐住」：這週是你要求的 DB/高併發/效能關鍵字核心週。

### 你要學的技能（DB/高併發/效能常用詞）

- SQL 基本功：JOIN/GROUP BY、pagination query pattern、避免 N+1（在 DB/ORM 層）
    
- Indexing：單欄/複合索引、selectivity、covering index（概念）
    
- Query planning：`EXPLAIN` / `EXPLAIN ANALYZE`（看成本、掃描方式）
    
- Transactions：ACID、isolation levels（Read Committed / Repeatable Read…概念）
    
- Concurrency control：locks、deadlocks（概念與避免策略）
    
- Connection pooling（例如 PgBouncer/ORM pool 概念）
    
- Idempotency（避免重試造成重複寫入）
    

### 交付成果

- 針對 1–2 條主要查詢：提供 `EXPLAIN` 截圖/輸出與優化說明（README）
    
- 加 1 個索引並驗證效能改善（有前後對照）
    
- 寫一份「交易/鎖/死鎖」風險筆記（面試版）
    

### AI 協助

- ChatGPT：根據 query/資料量假設提出索引建議（你再用 EXPLAIN 驗證）
    
- Claude Code：重構資料存取層，避免 N+1、補 pagination 正確性
    

---

## Week 9｜Reliability & Performance：高併發保護網（rate limit / cache / async）

### 本週目標

建立你作品的「抗壓能力」：面試時你能講清楚如何處理高流量與尖峰。

### 你要學的技能（可靠性/效能用語）

- Rate limiting / throttling（429 strategy）
    
- Timeout / retry / exponential backoff（何時可重試、何時不可）
    
- Caching：cache-aside/read-through（選一種最小落地）
    
- Async processing：job queue / background worker（概念 + 最小實作可選）
    
- Load testing 入門（k6 或等價工具概念即可）
    
- Profiling/Tracing 概念（先知道要看什麼）
    

### 交付成果

- 至少一個端點具備：timeout + 統一重試策略（或明確不重試）
    
- 針對高頻讀取：加一層快取（可用 in-memory/edge cache，先求合理）
    
- 產出「爆點清單 Top 3」：DB saturation、hot key、external API dependency + 對策
    

### AI 協助

- ChatGPT：根據你的流量假設做 capacity planning 粗估（QPS、DB 連線數）
    
- Claude Code：幫你把錯誤處理與 retry pattern 寫成可重用工具
    

---

## Week 10｜Automation 副線 MVP：n8n + LLM（結構化輸出 + 可維護）

### 本週目標

做出一條可重跑、可審核、可版本化的自動化流程（對你副業直接省時間）。

### 你要學的技能（工作流/LLM常用詞）

- Workflow orchestration：trigger / transform / review / publish
    
- Structured output：JSON schema、guardrails、fallback strategy
    
- Idempotency & deduplication（避免重複發文/重複寫入）
    
- Error handling branch + alerting（通知）
    
- Secrets & permissions（least privilege）
    

### 交付成果

- 1 條 end-to-end workflow：資料來源→摘要→產出文案(JSON)→Telegram 審核→入庫/發佈
    
- 匯出 workflow JSON + 版本註記
    
- Prompt v1（含示例、輸出 schema、錯誤處理規則）
    

### AI 協助

- ChatGPT：幫你設計 prompt（輸入→輸出 schema→few-shot 範例）
    
- Claude Code：幫你寫 n8n code node（清洗/去重/格式化）
    

---

## Week 11｜System Design 2：面試題模板化（2 題限時演練 + trade-offs）

### 本週目標

不只會講自己的專案，也能解通用 system design 題。

### 你要學的技能（面試高頻元件）

- CDN / Load Balancer（何時用、解什麼問題）
    
- Cache（read/write patterns）
    
- Queue（async + smoothing traffic）
    
- DB scaling 概念：replication / sharding（概念即可）
    
- Consistency / availability trade-off（入門）
    

### 交付成果

- 2 題限時 45 分鐘解題（例：URL shortener、news feed 簡化版）
    
- 每題都要有：需求澄清→架構→data model→bottleneck→trade-offs
    

### AI 協助

- ChatGPT：計時面試官 + 追問（hot partition、cache invalidation、DB 索引策略）
    
- Claude Code：幫你把解題稿整理成可讀文件（像真的面試筆記）
    

---

## Week 12｜求職化收斂：作品集/履歷可用輸出 + 12 週回顧閉環

### 本週目標

把成果「包裝成職缺語言」：關鍵字要出現在 README/履歷 bullet。

### 你要學的技能（職缺語言）

- Project storytelling（STAR/CAR）
    
- 技術決策敘事（trade-offs）
    
- 可量化成效：latency、成本、可靠性（可估算但要合理）
    

### 交付成果（最關鍵）

- Demo 影片（2 分鐘）
    
- README 最終版（setup、架構圖、ADR、性能/安全/可靠性策略）
    
- 履歷版 bullets（中/英文各一版）：
    
    - Next.js/React/TS、REST APIs、PostgreSQL（index/EXPLAIN）、Auth (session/JWT/OAuth)、Docker、CI/CD、observability、rate limiting/caching
        

### AI 協助

- ChatGPT：把你的中文經驗改寫成「海外履歷英文 bullet」並校正用字
    
- Claude Code：整理 README/ADR，補齊技術決策與限制/下一步
    

---

# 3) 後端與資料庫「關鍵字加強包」（你要求的詞彙我用正確領域用語整理成清單）

你在學習/履歷/README 會用到的高頻關鍵詞（也對應你 Week 4/8/9 的內容）：

## PostgreSQL / Database

- Data modeling, normalization / denormalization（視情境）
    
- Indexing strategy（single/compound, selectivity）
    
- Query planning: `EXPLAIN (ANALYZE)`, sequential scan vs index scan
    
- Transactions, ACID, isolation levels
    
- Concurrency control: locks, deadlocks, contention
    
- Connection pooling（pool sizing, saturation）
    
- Pagination: offset vs cursor pagination
    
- Migrations, schema evolution
    

## 高併發 / 可靠性

- Rate limiting / throttling（429）
    
- Timeout, retry, exponential backoff
    
- Idempotency keys
    
- Caching (cache-aside/read-through), cache invalidation（概念）
    
- Async processing / job queue / worker（可選最小落地）
    
- Load testing / stress testing（入門即可）
    
- Bottleneck analysis / capacity planning（粗估 QPS）
    

## 安全性（Security）

- AuthN/AuthZ（session/cookie, JWT, OAuth）
    
- RBAC
    
- OWASP top risks（XSS/CSRF/Injection）
    
- Input validation (schema validation)
    
- Secrets management, least privilege
    
- Audit logging（概念）
    

## 效能優化（Performance）

- N+1 query avoidance
    
- Profiling/observability（structured logs, request_id）
    
- DB tuning（索引、查詢重寫、减少 round trips）
    
- Frontend performance（core web vitals 概念、SSR/CSR trade-off）